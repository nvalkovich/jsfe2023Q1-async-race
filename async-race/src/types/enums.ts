export enum PageIds {
  Garage = 'garage',
  Winners = 'winners',
}

export enum InputCategories {
  Create = 'create',
  Update = 'update',
}

export enum EngineStatus {
  Start = 'started',
  Stop = 'stopped',
  Drive = 'drive',
}
